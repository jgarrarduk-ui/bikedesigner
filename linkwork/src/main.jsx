import React from "react";
import { createRoot } from "react-dom/client";
import Linkwork from "./Linkwork.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Linkwork />
  </React.StrictMode>
);
