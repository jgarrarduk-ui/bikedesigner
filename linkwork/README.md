# Linkwork

Bicycle suspension kinematics in the browser. General planar linkage solver — no
per-layout maths — plus inverse design from target leverage figures.

    npm install
    npm run dev      # http://localhost:5173
    npm test         # solver regression tests, must stay green

Read `CLAUDE.md` first. It carries the architectural decisions and the traps.
